name: docker-build-container.yaml

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  docker-build-container:
    runs-on: ubuntu-latest
    steps:
      - name: Build docker image
        uses: Senzing/github-action-docker-buildx-build@latest
        with:
          build-options: --build-arg SENZING_ACCEPT_EULA=I_ACCEPT_THE_SENZING_EULA
          image-repository: senzing/wrap-image-with-senzing-data
          password: ${{ secrets.DOCKERHUB_PASSWORD }}
          username: ${{ secrets.DOCKERHUB_USERNAME }}
